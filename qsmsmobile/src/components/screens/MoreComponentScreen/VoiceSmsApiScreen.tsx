import React from "react";
import { Text, View } from "react-native";

const VoiceSmsApiScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Bulk Email Screen</Text>
    </View>
  );
};

export default VoiceSmsApiScreen;
